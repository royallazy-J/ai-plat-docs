server {
    listen 80;
    server_name _;

    root /opt/ai-plat-docs/site;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:css|js|png|jpg|jpeg|svg|ico|json)$ {
        expires 1h;
        add_header Cache-Control "public";
    }
}
